export CONTEXT_TYPE="{{CONTEXT_NAME}}"
export REMOTE_HOST="${REMOTE_HOST:-user@hostname}"
export REMOTE_REPO="${REMOTE_REPO:-~/repo/{{CONTEXT_NAME}}}"
export K8S_NAMESPACE="${K8S_NAMESPACE:-default}"
