# Context: {{CONTEXT_NAME}} — customize this file
export CONTEXT_TYPE="{{CONTEXT_NAME}}"

# Service ports (override in registry instance env block)
export SVC1_PORT="${SVC1_PORT:-8080}"
export SVC1_URL="http://localhost:${SVC1_PORT}"
