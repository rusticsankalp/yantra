# Dev app context — local app with potential remote service dependencies
export CONTEXT_TYPE="{{CONTEXT_NAME}}"
export APP_PORT="${APP_PORT:-3000}"
export APP_URL="http://localhost:${APP_PORT}"
# DEPENDS_ON=wscs-dev  # uncomment if this context needs another running
